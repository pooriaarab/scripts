{
  "name": "scripts Cloud Environment",
  "install": "node --version && python3 --version && bash --version",
  "terminals": [
    {
      "name": "Verify",
      "command": "export PATH=\"$PATH:$PWD\" && node --test pr-standards.test.mjs assisted-by.test.mjs && ./hooks/pr-standards-guard.test.sh && python3 build-repo-prefixes.test.py && python3 worker-preview-provision.test.py && python3 worker-preview-access.test.py && python3 install-pr-hooks.test.py && ./adopt-branch-pattern.test.sh && ./pr-standards-rollout-labels.test.sh && ./fleet-ci-census.test.sh && ./append-point-scan.test.sh && ./merge-hotspots.test.sh",
      "description": "Run the full repo test suite (mirrors .github/workflows/tests.yml). Run this before opening a PR."
    }
  ]
}
